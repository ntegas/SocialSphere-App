# План завершения редизайна — по папке «Дизайн Android приложения (2)»

**Эталон дизайна (обновлён 2026-07-02):**
`C:\Users\USER\Downloads\Дизайн Android приложения (2)\`
- `Socialsphere Прототип.dc.html` — читаемый шаблон (48 секций-экранов, комментарии `<!-- === ИМЯ === -->`)
- `Socialsphere Prototype.html` — тот же макет бандлом: открывать в браузере для живой сверки
  (`python -m http.server` в папке + `http://127.0.0.1:PORT/...`, НЕ `file://`; getComputedStyle, не скриншоты)
- `Socialsphere Aurelia.dc.html` — предыдущая итерация, НЕ эталон
- `uploads/` — копия исходников проекта (для дизайн-тула), игнорировать

**Рабочая дисциплина — `AURELIA_REDESIGN_PROMPT.md` (цикл §5, DoD §9, дисциплина §10) — без изменений.**
Правило «не теряя функций»: макет = истина по СТИЛЮ, не по списку полей/функций (подтверждено
решениями владельца в прошлых циклах: FORD-блоки, сортировка Контактов и т.д.).

## Что УЖЕ закрыто (живой сверкой, см. память проекта — НЕ переделывать)

Map · ContactEdit · CompanyEdit · CalendarItemEdit · CalendarItemDetail · CommunicationTab ·
WorkTab · GiftsTab · CompanyDetail · Duplicates · CheatSheet · Contacts (+Filters-лист) ·
NotesTab · OverviewTab (FORD) · корневой фикс `MaterialTheme(colorScheme=aureliaColorScheme(...))`.

**ContactDetailScreen — шапка/табы, цикл ЗАВЕРШЁН (2026-07-02), запрошено владельцем
напрямую** («вверху две кнопки редактировать — дубль», «Заметки обрезается», «Обзор не
совпадает»): 1) карандаш в шапке и «⋯→Редактировать» вели в ОДНО и то же место
(`onNavigateToEdit()`) — дубль; карандаш теперь переключает ИНЛАЙН-правку ТЕКУЩЕЙ вкладки
(Обзор/Работа/Связь — у них есть `editing`-стейт; Подарки/Заметки — кнопка неактивна,
там своя система диалогов), «⋯→Редактировать» остался единственным входом в полную форму
редактирования контакта; 2) убраны 3 отдельные «Изменить» внутри самих вкладок (Overview/
Work/CommunicationTab) — были спрятаны за `if(editing)`, дублировали функцию и создавали
задвоенный отступ (см. `SOCIALSPHERE_KNOWLEDGE.md` §11/§14/§21); 3) таб «Заметки» обрезался
до «Заме» на реальном устройстве — сверка `getComputedStyle` дала 14sp/spacedBy(15dp)
против наших 15sp/spacedBy(20dp), поправлено + добавлен `horizontalScroll` как страховка
на ещё более узких экранах/крупном системном шрифте; 4) 4 круглые кнопки шапки переведены
на канонический `AureliaCircleButton`/`AureliaBackButton` (были локальные `Box`-копии —
нарушали правило «экраны не заводят своих копий» из `AureliaComponents.kt`), кроме
кнопки инлайн-правки — у неё 3-е «неактивное» состояние, которого нет в
`AureliaCircleStyle`, оставлена своим `Box`, но на тех же токенах (`neutralFill`/`brand`).
`compileDebugKotlin`+`assembleDebug`+`qa_check.py` зелёные (клин-сборка).

## Фундамент — СДЕЛАНО (2026-07-02)

- Токены обеих палитр выровнены 1:1 с живым прототипом (JS themeVars): light tx2/tx3 исправлены,
  добавлены tx4/sheet/neutralFill/goldLabel; dark bg `#100E0A`, card `#1B1813`, tx2–tx4, sheet `#221E18`.
- **`ui/theme/AureliaComponents.kt`** — канонический слой примитивов макета (аватары-градиенты,
  aureliaPress, круглые кнопки, карточка с inset-кольцом, стат-карта Playfair, заголовки секций,
  caps-подписи, пульс-точка, шторка r28). Правило: экраны НЕ заводят локальных копий.
- Первые консьюмеры: HomeScreen (кнопки/статы/заголовки/аватары/пульс; встречи-плитка следует
  акценту как `var(--ac)`), CalendarScreen и ContactsScreen/OverviewTab (дедуп палитры и золота),
  ContactDetailScreen (шторка «⋯» на AureliaSheet), AureliaCaption → алиас.

## ОСТАЛОСЬ (порядок = приоритет; один экран/пункт за цикл §5; ВСЁ строить на слое AureliaComponents)

### 1. Экраны с устаревшим 🟢-статусом — пересверить живым макетом (статус-прокси лгал в обе стороны)
- [x] **HomeScreen** — СДЕЛАНО 2026-07-02 (на слое AureliaComponents: кнопки/статы Playfair/
      заголовки/аватары/пульс-точка; плитка «встречи» следует акценту).
- [x] **CalendarScreen** — СДЕЛАНО 2026-07-02: трек сегментов → neutralFill; Лента — группы
      по датам («Сегодня · 27 июня» акцентом, далее «2 июля»…), caps-заголовки, линия
      акцент→золото, точка «сегодня» с пульсом; Неделя — плитки h44/r12 без карточки-обёртки,
      события «время в гаттере + карточка с цветным бортом 3px» (WeekEventRow); Месяц — без
      карточки-обёртки, шапка Playfair со стрелками справа, caps-заголовок дня.
      Компиляция + qa зелёные. Функции сохранены (фильтры, скрытые типы, ДР-проекция).
- [x] **CompaniesScreen** — СДЕЛАНО 2026-07-02: «+» на AureliaCircleButton; хардкоды
      0x17787880/0x1F767680 → токен neutralFill; чип сортировки активный — акцент-заливка
      с белым (был только цвет текста); сегмент-чипы — inset-кольцо у неактивных; добавлен
      счётчик «N компаний» (строка comp_count уже существовала — дубль пойман qa и убран);
      лого 46/r14 на общей палитре AureliaAvatars.companyGradients (добавлена в слой:
      малахит/синий/охра/слива/тил из прототипа); пилюля «N чел.» r11/W700.
      Функции сохранены (поиск, сорт, фильтры индустрии/города). Компиляция + qa зелёные.
- [x] **SettingsScreen** — СДЕЛАНО 2026-07-02: шапка на AureliaBackButton + Playfair 28
      (у AureliaScreenTitle появился параметр fontSize: корневые 34, push 28); плитка
      «Уведомления» → тревожный #C0492F (в прототипе НЕ терракот); IconTile 30→32 (глобально);
      шеврон строк → токен quaternaryLabel (tx4), 17dp; заголовок строки SemiBold.
- [x] **Свип шапок push-экранов** — СДЕЛАНО 2026-07-02: все 8 (Appearance, CalendarSettings,
      Duplicates, ImportExport, Language, Notification, Privacy, ScanCard) переведены на
      AureliaBackButton (нейтральная заливка + шеврон + press-анимация) и Playfair 28.
      Компиляция + qa зелёные. Контент этих экранов сверяется отдельными циклами ниже.

### 2. Экраны настроек (5 шт., в реестре §4 как «не начаты»)
- [x] Appearance — СДЕЛАНО 2026-07-02: контент уже совпадал 1:1 (превью тем 120/r18, круги
      акцентов 44 с кольцом, кнопка-превью); локальный CapsLabel → обёртка AureliaCapsLabel.
- [x] Notifications — СДЕЛАНО 2026-07-02: NotifCard уже был по паттерну «caps снаружи + карта»
      (caps → обёртка AureliaCapsLabel); SwitchRow получил опциональную плитку-иконку 30/r8 —
      мастер «Включить» с колокольчиком на alarmRed (якорь прототипа); подписи SemiBold.
      Вся богатая функциональность (мультивыбор ДР, «пора связаться», точные будильники) сохранена.
- [x] Language — СДЕЛАНО 2026-07-02: совпадал; неактивная плашка Ru/En/Ελ → neutralFill.
- [x] CalendarSettings — СДЕЛАНО 2026-07-02: сегменты Пн/Вс — inset-кольцо неактивному;
      «Видимость типов» — caps-заголовок снаружи карточки (как в прототипе), подписи SemiBold;
      УДАЛЁН блок «Цвета» — легенда показывала выдуманные цвета (brand@10%/fill/card, не
      реальные цвета типов) = обманка §2; настоящие цвета видны в «Видимости». Режим по
      умолчанию (наша функция сверх макета) сохранён.
- [x] Privacy — СДЕЛАНО 2026-07-02: новый токен alarmRed (#C0492F light / #E0846E dark) —
      «опасная зона» (caps, кольцо, иконка, заголовок) и плитка «Уведомления» в Настройках
      теперь на нём; карта «Локальное хранение» получила акцент-кольцо .16; SectionCaps →
      обёртка AureliaCapsLabel. Блок «Разрешения Android» сохранён (аддитивная функция).

### 3. Импорт
- [ ] ImportExportSettingsScreen (`IMPORT / EXPORT`)
- [ ] ImportScreens: pick/preview/duplicates (`IMPORT PICK` / `IMPORT PREVIEW` / `DUPLICATES RESOLVE`)

### 4. НОВЫЕ экраны прототипа (в приложении отсутствуют)
- [ ] **SEARCH** (`SEARCH`) — глобальный поиск: строка+Отмена, сегменты Все/Контакты/Компании/
      События, подсветка совпадения (gold @30%), секции «Контакты · N». Точки входа: кнопка на
      Home и поле-плейсхолдер на Contacts. Данные уже есть в AppStateStore.
- [ ] **CONTACT HISTORY** (`CONTACT HISTORY`) — таймлайн взаимодействий контакта
      (звонки/встречи/подарки из notes+calendarItems). Вход — с карточки контакта.
- [ ] **PIN ACTION SHEET** (`PIN ACTION SHEET`) — шторка пина карты: аватар+адрес+расстояние,
      Открыть карточку / Построить маршрут / Позвонить / Отмена. Сейчас на карте `MapItemDetailCard` —
      заменить/оформить шторкой по макету.

### 5. Сканер (доделать незакоммиченный WIP в дереве)
- [ ] Привязать `CardCameraScanner` + `BusinessCardOcr` (уже в дереве, собираются) к `ScanCardScreen`:
      камера (`SCANNER`) → OCR → существующий разбор/правка полей, сверить с `SCAN RESULT`;
      экран разрешения — с `PERMISSION`. Строки scan_camera_permission/scan_frame_hint/scan_capture (ru/en/el).
- [ ] Закоммитить весь сканерный WIP (build.gradle.kts, манифест, .gitignore, toml, settings.gradle.kts).

### 6. Онбординг — рестайл под секцию `ONBOARDING`
- [ ] Фон `#13261D` (не `#0E0D0A`), два декоративных круга (малахит/золото @12-16%),
      wordmark letterspacing .32em gold, Playfair 40 с курсивным «каждого» цветом `#5FB894`,
      точки-пейджер, кнопка `#5FB894` с тёмным текстом.

### 7. Финальная полировка (после всех экранов)
- [ ] Анимации макета: press-scale (.96), каскадное появление (stagger), пульс-точки, кольцо аватара.
      Сначала общие модификаторы в дизайн-системе, потом точечно по экранам.
- [ ] Тёмная тема: сверка по секции `DARK HOME`; выровнять dark-токены bg `#100E0A` / card `#1B1813`
      (сейчас `#0E0D0A`/`#1A1813`) — ЕДИНОЙ правкой в `AppleDesignSystem.kt`/`AureliaDesignSystem.kt`.
- [ ] Новые токены при необходимости: `--tx3:#A79F90`, `--tx4:#C4BDB0`, `--sheet:#F4F1EA` —
      добавлять только когда экран реально их требует (не заранее).
- [ ] Прогон DoD §9: light+dark × 4 акцента, qa_check, assembleDebug.

## Как резать секции прототипа (повторяемо)
```bash
cd "Дизайн Android приложения (2)" && awk -v out="ПАПКА" '
BEGIN{n=0; fn=out"/00_preamble.html"}
{ if (match($0, /<!-- =+ [A-ZА-Я \/]+ =+ -->/)) { name=substr($0,RSTART,RLENGTH);
  gsub(/<!-- =+ /,"",name); gsub(/ =+ -->/,"",name); gsub(/[ \/]+/,"_",name);
  n++; fn=sprintf("%s/%02d_%s.html",out,n,name) } print >> fn }' "Socialsphere Прототип.dc.html"
```
